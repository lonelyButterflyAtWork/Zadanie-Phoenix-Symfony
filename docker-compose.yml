version: '3.8'

services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: phoenix_app
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  phoenix:
    build: ./phoenix_api
    command: mix phx.server
    environment:
      MIX_ENV: dev
      DATABASE_URL: ecto://postgres:postgres@db/phoenix_app
    volumes:
      - ./phoenix_api:/phoenix_api
    ports:
      - "4000:4000"
    depends_on:
      - db

  symfony:
    build:
      context: ./symfony_app
      dockerfile: Dockerfile
    volumes:
      - ./symfony_app/app:/var/www/html
    ports:
      - "8000:80"
    depends_on:
      - phoenix


volumes:
  db-data:
